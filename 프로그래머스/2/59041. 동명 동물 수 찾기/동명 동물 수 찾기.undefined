SELECT NAME, COUNT(NAME) AS COUNT
FROM ANIMAL_INS
WHERE NAME IS NOT NULL
GROUP BY NAME
HAVING COUNT(NAME) >= 2
ORDER BY NAME ASC;

# ANIMAL_INS에서
# 동물 이름 중 두 번 이상 쓰인 이름, 해당 이름이 쓰인 횟수 조회
# 이름이 없는 동물은 제외
# 이름 ASC